baseURL = "http://localhost:1313/"
languageCode = "en-us"
title = "My Hugo Site"

[outputs]
home = ["HTML", "JSON"]

[build]
  command = "hugo"
  publish = "public"

[build.environment]
  # pick a Hugo version that matches your local dev environment (example)
  HUGO_VERSION = "0.115.1"


