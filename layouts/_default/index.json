[
{{- $posts := where .Site.RegularPages "Section" "blog" -}}
{{- range $index, $page := $posts -}}
  {{- if gt $index 0 }},{{ end }}
  {
    "title": "{{ .Title }}",
    "url": "{{ .RelPermalink }}",
    "content": {{ .Plain | jsonify }}
  }
{{- end -}}
]
